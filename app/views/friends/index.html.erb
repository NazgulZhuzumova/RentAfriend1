<h1>There are <%= Friend.count %> Friends Available Now</h1>
<ol>
  <% @friends.each do |friend| %>
  <li> <%= link_to friend.name, friend_path(friend) %></li>
  <% end %>
</ol>
<%= link_to "Back to homepage", root_path %>
